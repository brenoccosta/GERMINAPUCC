<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/login.css" rel="stylesheet">
    <script src="/js/login.js" defer></script>
    <link href="/css/produtos.css" rel="stylesheet">
    <link href="/css/slider.css" rel="stylesheet">
    <link href="/css/fundo.css" rel="stylesheet" >
    <link href="/css/menu_topo.css" rel="stylesheet" >
    <title>GERMINA | Produtos</title>
</head>
<body>
  <%- include('./partials/menu_topo') %> 
  <%- include('./partials/login') %>
  <%- include('./partials/slider') %>
  <div class="corpo_interno">
    <% for(let produto of produtos) { %>
    <div class="moldura">
      <div class="noticia1_container">
        <img class="noticia1_mask" src=<%= produto.path_img %> >
        <div class="overlay">
          <div class="text">
            <h4><%=produto.nome %> (<%=produto.Descricao %>)</h4>
            <table>
              <tr>
                <th>Data</th>
                <th>R$</th>
                <th>Variação/Dia</th>
                <th>Variação/Mês</th>
              </tr>
              <% for(let i = 0; i < 8; i++) { %>
              <tr>
                <td id="data"><%=produto.precos[i].data %></td>
                <td><%=produto.precos[i].valorReal %></td>
                <td><%=produto.precos[i].variacaoDiaria %>%</td>
                <td><%=produto.precos[i].variacaoMensal %>%</td>
              </tr>
              <% } %>
            </table>
          </div>
        </div>
       </div>
    </div>
    <% } %>
  </div>

<%- include('./partials/fundo') %> 

</body>
</html>
